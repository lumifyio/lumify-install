#
# Whitelist of certnames or domain name globs (one per line) whose certificate requests will automatically be signed.
#
# This file should be modified during installation to specify only hosts that should be accessing the puppet master.
#

*.vm.local
*.ec2.internal
